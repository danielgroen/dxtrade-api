export enum ORDER_TYPE {
  MARKET = "MARKET",
  LIMIT = "LIMIT",
  STOP = "STOP",
}

export enum SIDE {
  BUY = "BUY",
  SELL = "SELL",
}

export enum ACTION {
  OPENING = "OPENING",
  CLOSING = "CLOSING",
}
